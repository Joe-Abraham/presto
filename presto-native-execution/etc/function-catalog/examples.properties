# Example Function Catalog Configuration
# This is an example catalog for demonstrating function catalog features

# Example configuration property
example.prefix=ExamplePrefix

# Additional example properties
example.case-sensitive=true
example.max-length=1000
example.timeout-ms=5000

# This catalog can be used by functions that need shared configuration
# Functions can access these properties via FunctionCatalogManager
