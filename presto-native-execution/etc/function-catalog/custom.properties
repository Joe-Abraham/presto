# Custom Function Catalog Configuration
# This catalog demonstrates organizing custom functions with shared configuration

# Mark this as a function catalog (optional if no connector.name is specified)
# connector.name=function-catalog

# Example configuration properties for custom functions
# These properties can be accessed by functions at runtime through the
# FunctionCatalogManager and can be overridden by session properties

# Example: Custom configuration for custom functions
# custom.case-sensitive=false
# custom.locale=en_US
# custom.max-length=1000
