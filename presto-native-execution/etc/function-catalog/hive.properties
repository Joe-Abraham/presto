# Hive Function Catalog Configuration
# This catalog organizes Hive-specific functions with shared configuration

# Mark this as a function catalog (optional if no connector.name is specified)
# connector.name=function-catalog

# Example configuration properties for Hive functions
# These properties can be accessed by functions at runtime through the
# FunctionCatalogManager and can be overridden by session properties

# Example: Custom configuration for Hive functions
# hive.function.case-sensitive=false
# hive.function.locale=en_US
