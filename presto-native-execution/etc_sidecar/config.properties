discovery.uri=http://127.0.0.1:<replace_port>
presto.version=testversion
http-server.http.port=7778
shutdown-onset-sec=1
runtime-metrics-collection-enabled=true
native-sidecar=true
presto.default-namespace=native.default

# Native Sidecar Configuration
# These settings control the native sidecar function registry behavior
#
# To enable catalog filtering in function namespace managers,
# add sidecar.catalog-name property to function-namespace/*.properties files
#
# Example function namespace configurations are available in:
# function-namespace/native.properties (all functions)
# function-namespace/native-hive.properties (hive catalog only)
# function-namespace/native-presto.properties (presto catalog only)
